using System.Collections.Generic;
using UnityEngine;

public class TutorialController : MonoBehaviour
{
    /*public List<TutorialClaster> TutorialsClusters = new List<TutorialClaster>();
    private int currSelectedTutorialCluster = -1;

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.K))
        {
            GoToNextTutorialInCluster();
        }
    }

    public void StartTutorial(string eventName)
    {
        currSelectedTutorialCluster = TutorialsClusters.FindIndex(t => t.EventName == eventName);
        if (currSelectedTutorialCluster >= 0)
        {
            TutorialsClusters[currSelectedTutorialCluster].GoToNextTutorialInClaster();
        }
    }

    public void GoToNextTutorialInCluster()
    {
        if (currSelectedTutorialCluster >= 0)
        {
            TutorialsClusters[currSelectedTutorialCluster].GoToNextTutorialInClaster();
        }
    }*/
}
